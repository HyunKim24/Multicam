'''
2. 여러개 데이터 (연속 데이터, 시퀀스 데이터)
 - 리스트    : [], 순서가 존재, 인덱스가 존재(0,1,2~..), 값의 중복허용
 - 딕셔너리  : {}, 순서가 없다, 데이터자체가 키:값의 세트로 구성
                  오직 키만으로 구분, 키값은 중복되면 안됨
                  키에 매칭되는 값은 중복 ok 
 - 튜플      : (), 순서가 있다, 인덱스 존재, 값의 중복 신경 안씀
                  값을 묶는다에 의미를 둔다.
 - 집합      :     값의 중복 제거시 사용(값은 유니크 고유하다.) 
                주로 리스트의 중복제거시 사용됨

'''
